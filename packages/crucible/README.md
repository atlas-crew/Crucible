# @atlascrew/crucible

Install the published package and start the unified Crucible runtime:

```bash
npm install -g @atlascrew/crucible
crucible start
```

The packaged runtime serves the web UI, API, and WebSocket traffic together on one port.
