# Crucible

Next-generation security testing platform.

## Packages

- **@crucible/catalog** — Scenario schemas, validation, and in-process loader
- **@crucible/demo-dashboard** — Attack simulation orchestrator with scenario engine

## Development

```bash
pnpm install
pnpm -r build
pnpm -r test
```
